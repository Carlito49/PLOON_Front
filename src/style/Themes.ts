import { StyleSheet } from "react-native";

const cc = 5;

const Themes = StyleSheet.create({
    default: {
        borderWidth: 3
    },
    validationBtn: {
        marginBottom: 10,
        width: '55%',
        alignItems: 'center',
    }
    
})

export default Themes;