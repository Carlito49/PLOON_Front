import React from 'react';
import { StyleSheet } from 'react-native';

const LienStyle = StyleSheet.create({
    container: {
        fontStyle: "italic",
        textDecorationLine: 'underline',
        textDecorationColor: 'blue'
    }
})

export default LienStyle;