import React from "react";
import { View } from 'react-native';

const SearchEvent: React.FunctionComponent = () => {
    return (
        <View>
            
        </View>
    )
};

export default SearchEvent;